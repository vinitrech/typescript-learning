let score: number | string = 33
score = "33"

const data: (string|number)[] = ["1", 2, 3] // correct way of creating a 2-type array

let seatAllotment: "aisle" | "middle" | "window" = "aisle" // define variable that can only have the specified values
