// const AISLE = 0
// const MIDDLE = 1
// const WINDOW = 2

enum SeatChoice {
    AISLE, // the value can be assigned, then the next will be value + 1
    MIDDLE,
    WINDOW
}

const seat = SeatChoice.AISLE
const middle = SeatChoice.MIDDLE
const window = SeatChoice.WINDOW

export{}